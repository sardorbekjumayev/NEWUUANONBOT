{
  "name": "pu/anonymous-stateless-bot",
  "description": "Privacy-first stateless anonymous Telegram bot for university communities.",
  "type": "project",
  "license": "proprietary",
  "require": {
    "php": "^8.3",
    "vlucas/phpdotenv": "^5.6",
    "guzzlehttp/guzzle": "^7.9"
  },
  "autoload": {
    "psr-4": {
      "PUAnonymous\\": "src/"
    }
  },
  "scripts": {
    "check": "php -l index.php && php -l setup.php && php -l config/config.php && find src -name '*.php' -print0 | xargs -0 -n1 php -l"
  }
}
